/**
 * DEMO DOMAIN — replace this entire package when building your own app.
 *
 *   schema/     → TinyBase table schema and TypeScript types
 *   operations/ → domain functions (addTodo, toggleTodo, deleteTodo)
 *   access.ts   → role-to-writable-tables access control config
 *
 * Infrastructure (packages/data) is generic — no edits needed for domain changes.
 */
export * from './schema';
export * from './operations';
export * from './access';
export * from './crypto';
