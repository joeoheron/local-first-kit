ALCHEMY_PASSWORD=please-change-this

# JWT signing secret — auto-generated on first `npm run dev` if left empty.
# For staging/prod: set as a Cloudflare secret (wrangler secret put JWT_SECRET).
JWT_SECRET=
